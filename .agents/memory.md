# Project Memory

## Current Activity

Current project and its sub-work items that we are working on actively. Items move to "Completed Work Items" when done.

### Project: Swarm-orchestration-service (this repo, bootstrapped 2026-09-10)

- **Repo purpose**: implement [`docs/plans/completed/orchestrator-service-simplification.md`](../docs/plans/completed/orchestrator-service-simplification.md) (APPROVED; all §5 decisions folded; §6 unknowns resolved by the spike) — PromptInfo queue feeding an ACP host replacing the old `orchestrator-service` dispatch. Requirements source: [`docs/plans/completed/orchestrator-service-integration.md`](../docs/plans/completed/orchestrator-service-integration.md). Listener/GitHub-auth code ported from `nam20485/orchestrator-service` @`2bd6d06` (reference only, untouched).
- **Phase 0 (overnight delegated run, 2026-09-10)**: 0.0 and 0.1 LANDED (see Completed); 0.2 PromptInfo design doc landed this PR ([`docs/plans/completed/promptinfo-design.md`](../docs/plans/completed/promptinfo-design.md)) — typed export-ready envelope, in-process `asyncio.Queue`, delivery-id dedup, EventStore fed `prompt_queued`/`prompt_consumed`/`webhook_duplicate`, explicit no-durability posture (GitHub redelivery is the recovery path), consumer contract Phase 1→2. Also mirrors the owner directive "any failing workflow ⇒ run `fix-failing-workflows` skill" into source-control rules.
- **Phase 1 — queue seam LANDED** (PR #7, merge `f5df9d1`, CI green; cold-verified by coordinator): new `src/webhook_receiver/prompt_queue.py` — frozen pydantic `PromptInfo` (all design-doc §2 fields; consumed state derived via `model_copy` since frozen) + `PromptQueue` (unbounded `asyncio.Queue`, bounded dedup deque(maxlen=1024)+set with evict-before-append sync, `enqueue -> bool`, `consume()` placeholder marks consumed + emits `prompt_consumed {ok}`, drops on exception, no retry). `app.py` wired: `create_app(settings, event_store, prompt_queue=None)` injection param; lifespan starts the consumer task and cancels+awaits it on shutdown; accepted path enqueues, emits `prompt_queued` (caller) / `webhook_duplicate` (caller on dedup drop), 202 bodies/status unchanged. pytest 62 @ 96.24% (prompt_queue 100%); full `validation.ps1` green; boot smoke green (`OS_WEBHOOK_SECRET=test` → /health 200, bad signature 401, clean lifespan shutdown).
- **Phase 2 — ACP host LANDED (PR #9 merge `e2855c1` + CI fix #10 `039f1af`; cold-verified by coordinator incl. stripped-env CI-sim)**: new `src/webhook_receiver/acp_policy.py` (fail-closed permission policy: deny-regex → reject, read/search/think/fetch kinds → `allow_always`, unclassified → `acp_default_permission` reject|allow_once; no matching option → `DeniedOutcome(cancelled)`) and `src/webhook_receiver/acp_host.py` (one cold `opencode acp --cwd <scratch>` session per PromptInfo via `agent-client-protocol==0.12.1`; timeout ladder step×3+prompt with kill-on-exit + shielded reap; workspace `/tmp/swarm-acp-workspaces/<envelope id>` with belt-and-braces `opencode.json` deny-list; EventStore contract `agent_session_started`/`agent_message_chunk`/`agent_tool_call`/`agent_usage`/`agent_permission`/`agent_finished` — all carry `run_id`; Decision 10 honored: zero host-side model pins). `PromptQueue(store, host=None)` + `create_app` attach an `AcpHost` only when the app owns the queue and `acp_enabled`; no host ⇒ Phase 1 placeholder unchanged. Settings gained `acp_*` env knobs (`ACP_ENABLED/OPENCODE_BIN/WORKSPACE_ROOT/STEP_TIMEOUT/PROMPT_TIMEOUT/DEFAULT_PERMISSION/DENY_PATTERNS/DENIED_TOOLS`; deny patterns validated at boot). `acp_smoke.py` = manual real-opencode entry (`PYTHONPATH=src python -m webhook_receiver.acp_smoke`). Verified: pytest 121 @ 96.84%; full `validation.ps1` green; boot smoke green; **live e2e**: signed `issues.labeled` webhook → 202 → real opencode session → `end_turn` in ~6 s, `pgrep opencode` clean. Phase 3 seam: `AcpHost.run` prefers `info.prompt` when present; until then `build_prompt` derives a pipeline-check instruction (replace in Phase 3).
- **Phase 3 — open-ended orchestration prompt LANDED (PR #12 merge `fcbd881`, CI green; cold-verified by coordinator — real-opencode smoke re-run, live gh state queries observed)**: new `src/webhook_receiver/prompt_builder.py` — `build_orchestration_prompt(info)` string composition per plan §3.3 (no templating engine): one shared workflow template (envelope context + key-sorted EVENT_DATA JSON, live-tracking-state progression via `gh`, new-app vs existing-feature path DISCOVERY per §3.3 paths a/b, publish & verify: non-default branch/PR/labels, never default branch, remote-reachability before done) plus a per-label-class direction — `gh-issue-tracking:*` hierarchy workflows, `orchestration:*` lifecycle workflows (incl. body-named dynamic dispatch), `implementation:ready|complete` implementation path, unmatched → report-no-matching-workflow (old `(default)` clause), and `gh-issue-tracking:direct-body` → ENTIRE issue body verbatim (sender gate stays in filters; matches on the issue's full label set like the old clause; empty body → sentinel direction). Old repo's 431-line clause table (@2bd6d06) used as the regression checklist — matrix tests assert each label's equivalent open-ended direction and NO next-label chaining (no other table label literal in any prompt). Wired in `app.py`: accepted path fills `info.prompt` at enqueue (frozen `model_copy`); `AcpHost.build_prompt` seam unchanged (filled envelope prompt wins; derived pipeline-check text now fallback-only). `acp_smoke` defaults to the built prompt (`--prompt` overrides); `validation.ps1` python coverage list gained `webhook_receiver.prompt_builder`. Verified: pytest 148 @ 97.1% (prompt_builder 100%) in BOTH normal and stripped env (`env -i` CI sim); full `validation.ps1` green; boot smoke green (health 200 / bad-sig 401 / clean shutdown); **real-ACP smoke**: built `orchestration:plan-approved` prompt through real opencode 1.18.30 → model queried live tracking state (`gh repo view`/`gh issue list`/`gh pr list`), found no plan hierarchy, took no action, acknowledged, `end_turn`.
- **A2A protocol research LANDED (2026-09-10)** (write-up was never committed to git; the findings live in this entry and in Decisions): spec v1.0.1 (Linux Foundation, proto-normative, 3 bindings, AgentCard discovery, Task lifecycle, SSE + webhook push). Verdict: **anti-fit** inside the ZCode swarm (can't lift depth-1; workers stay least-privilege; file board already covers co-tenancy) and for local opencode control (keep ACP — request_permission + tool_call streaming are the fail-closed properties A2A lacks); **fit only as an optional external facade** on the FastAPI service (Python `a2a-sdk` 1.1.2 stable, mounts as routes). a2a-dotnet = all-prerelease, no gRPC, mapped to SwarmSandbox but deferred. Layering adopted: MCP (tools) / ACP (local steering) / A2A (network delegation). Layering stands as analysis only (MCP tools / ACP local steering). **Owner verdict 2026-09-10: not a fit — dropped entirely**; no triggers, do not re-open without owner direction.
- **Phase 4 — sandbox bridge + paths (a)/(b) COMPLETE (2026-09-10, branch `dev/phase4-sandbox-bridge`)**: bridge per [`phase4-bridge-design.md`](../docs/plans/completed/phase4-bridge-design.md) option (iii) — SwarmSandbox provisions, `sandbox_bridge.py` materializes the host workspace via docker cp (token-free, origin removed, harness-asset-verified), `AcpHost` drives opencode in it, release on session end (shielded; reaper backstop); `SANDBOX_*` settings with fail-closed `prompt_consumed {ok:false}` when unreachable and plain-scratch fallback when disabled. Suite 177 @ 97.6% green in both envs. **Exit criterion demonstrated first-hand on `nam20485/swarm-orch-demo-scratch`** (retained as evidence; deletion needs `delete_repo` scope): path (a) `demo-path-a-4` — session wrote `plan_docs/application_plan.md` and ran gh-issue-tracking-init → Plan/Epic/Stories #8–13 with `gh-issue-tracking:init-success`; path (b) `demo-path-b-1` (queued FIFO behind a) — discovered the existing hierarchy and filed Epic 2 #15 + Story 2.1 #16 under it, no re-init (Decision 9). Boundary (honest): one 60-min session covers plan+init and feature filing, not full implementation; swarm stays ZCode-native (§6) — opencode sessions drive the assets' entry points, full swarm execution is harness-gated. Robustness proven: timeout releases cleanly, SIGINT cancels + releases mid-session, detached (setsid) pipeline survives host-app force-closes. SwarmSandbox AppHost still blocked in this env (DCP k8s-watch timeouts) — standalone API (built DLL + env conn string) is the working pattern; postgres via local container (stopped post-demo).
- **Phase 5 — hardening & docs COMPLETE (2026-09-11, branch `dev/phase5-hardening` off 3dd87de, commits b3cab3f/6ce5b16/c00e23c/dc95fa7/21a4a5c/2e0fe53 + memory)**: version 0.2.0 (app + ACP client_info); **`GET /events` SSE** streams the EventStore (replay + live fan-out + keepalive comments, new `WEBHOOK_EVENTS_KEEPALIVE` knob default 15s; stable 15-event contract table in app.py module docstring; tests bind a real uvicorn on an ephemeral port because starlette 1.6 TestClient buffers whole responses — infinite SSE deadlocks it); **`scripts/e2e-orchestration.ps1`** hermetic simulator smoke (~3s, 21 assertions, no docker/opencode): boots the listener on loopback ACP_ENABLED=false and drives accept/ping/401/filtered/duplicate asserting statuses + EventStore outcomes over the /events SSE surface; wired into `validation.ps1` as `-Step e2e` (runs in `-Step all` after python, graceful skip without venv); **docs**: root README rewritten for the service (all env knobs table incl. DIRECT_BODY_ALLOWED_SENDERS, run/compose/test, template-lineage note per Decision 8) + new `src/webhook_receiver/README.md` (flow, event contract linked to docstrings, tier-0/1/2 CI note) + AGENTS.md identity/validation sections updated; **`docs/plans/old-stack-decommission.md`** = plan-only cutover/rollback/checklist for retiring orchestrator-service (old repo frozen read-only @2bd6d06). Scanner hygiene: test_config placeholder value switched to the scanner-recognized FAKE literal; one `.secretscan-allow` line rule added for the smoke's variable-only secret env assignment. **CI Tier 1 (opencode handshake) SKIPPED** per owner parking + task criterion: upstream install script has no checksum verification yet (anomalyco/opencode#31390 open), so per-PR SHA-pinned deterministic install isn't cleanly implementable; drift remains covered by the pinned `agent-client-protocol==0.12.1` + phase-gate `acp_smoke` runs. Verified: pytest 182 @ >97% in normal AND stripped env; `-Step build/-Step python/-Step e2e` green; full gate at final commit.
- **Next (superseded)**: PR #15 (`dev/phase5-hardening`) merged `5648759` 2026-09-11 — the service is feature-complete per plan §7; [`docs/plans/old-stack-decommission.md`](../docs/plans/old-stack-decommission.md) executes only on owner direction.
- **Guidance docs (2026-09-11, branch `dev/service-guides`)**: owner asked for description/components/usage docs after the operational Q&A (funnel, compose parity, dashboard status). Added `docs/architecture.md` (description, component inventory incl. where opencode runs, delivery-flow diagram, deployment topology, design-record links) and `docs/usage.md` (run-mode table incl. the honest "compose image has no opencode ⇒ listener-only" caveat, funnel setup, GitHub App wiring, label trigger table, `/events` observation walk-through, troubleshooting, no-durability/redelivery posture). Linked from root README (Guides line + layout table).
- **`.env.example` (2026-09-12, working tree — commit pending owner direction)**: owner noticed no example env existed despite the documented knob count. New root `.env.example` covering the full 21-var surface: all 19 `config.py` knobs + `DIRECT_BODY_ALLOWED_SENDERS` (live-read in `filters.py`) + `WEBHOOK_SITE_ADDRESS` (compose/Caddy-only, not read by the service), grouped Required / Conditionally required (`SANDBOX_API_URL` when `SANDBOX_ENABLED=true`) / Optional-by-area / Deployment-only; optional vars commented with defaults, only `OS_WEBHOOK_SECRET=` live. Pointers added: compose.yaml setup comment, README compose run block, usage.md §2. `.gitignore` already had `!.env.example` — the file was anticipated, never created. **Follow-up same day (owner "yes")**: compose.yaml `environment:` now also forwards the listener knobs `WEBHOOK_MAX_BODY_BYTES` (26214400), `WEBHOOK_EVENTS_KEEPALIVE` (15), `DIRECT_BODY_ALLOWED_SENDERS` (empty) — defaults duplicated from `config.py` with a keep-in-sync comment; `ACP_*`/`SANDBOX_*` deliberately NOT forwarded (image has no opencode/docker — would be dead knobs). `.env.example` header/reminder updated: host run leads (every knob live; `set -a; . .env; set +a` hint), compose forwards exactly those five. Owner challenge "why keep .env.example if vars modify nothing" resolved: premise held only for the old compose passthrough; host runs (the dispatch mode) honor all 21. **Landed as PR #18** (3 commits; CI green 2026-09-12; merged `8b418b0` 2026-09-15).
- **New-app launch pipeline folded in from `workflow-launch2` (2026-09-15, branch `dev/fold-launcher`, plan [`docs/plans/completed/fold-workflow-launch2-into-service.md`](../docs/plans/completed/fold-workflow-launch2-into-service.md) — MERGED 2026-09-16 as `632791a` (owner merged out-of-band after both review rounds; head `941d7f4`; all 12 review threads replied + resolved; remote branch deleted))**: the launcher and the service that consumes its output are now one repo — `create-repo-agent-context.ps1 -Slug <slug>` and its whole transitive closure moved in. **`ddc6037`** imports 9 PowerShell scripts + `.github/.labels.json` **byte-verbatim** (no logic changes, so `git log --follow` survives); **`8703152`** imports the two launcher support docs (`docs/wsl-ssh-agent-setup-guide.md` — a hard prerequisite, `repo-functions.ps1`'s `Invoke-GitClone` clones over `git@github.com:` — and `docs/plans/completed/orchestration-cycle-label-trigger-options.md`, the 2026-07-21 design history for the label-driven cycle this service now implements) and gitignores `logs/` (`logging.ps1`'s `Start-RunLog` writes one timestamped JSONL per run there); **`efc4350`** adds `../workflow-launch2` to the workspace folders; **`490b260`** repoints the imported scripts' doc comments at this repo; **`b37b607`** carries the three behaviour changes; **`07e0dff`** lands the repo-root `tests/` suite; **`fe7a0ee`** wires `tests/` into the gate and documents the pipeline in `.agents/rules/scripts.md` + the `AGENTS.md` Scripts brief; **`982eef8`** fixes `import-labels.ps1` so `-DryRun` tolerates a repo that does not exist yet. **Landed as PR #20** against `development` (milestone `maintenance`, `validate` SUCCESS 2026-09-15; merged `632791a`). The PR #20 Kilo review (2026-09-15) found 7 defects, all fixed in **`ad48b21`** with every thread replied + resolved: CRITICAL — the post-clone seed-commit amend was never pushed, so `origin/main` kept the template's ask-permissions and model pins (the amend block now force-pushes `--force-with-lease origin HEAD:main`); stage 1 completed without `exit 0`, leaving the wrapper's `$LASTEXITCODE` check at the mercy of stray native commands; `New-RepoSecret` threw on unset `GEMINI_API_KEY` *before* any dry-run guard; unguarded `New-Item` calls made `-DryRun` create clone dirs; plus the labels-superset doc claim, an unreachable `-DryRun` help exemption, and a policy-failing example. A **second Kilo pass the same day** (2026-09-15) found 4 more — three fixed in **`faf8c47`**, the fourth (this bullet's own counts) in **`1591f7a`** — with every thread replied + resolved: the `git add .` between the seed-commit amend and the force-push was not exit-code checked (a failed add — classic `.git/index.lock` contention — would have let the amend succeed on the stale index and publish the pre-cleanup tree, the exact CRITICAL the guard chain closes); the multi-repo `-DryRun` integration test could false-green (`Should -Not -Throw` cannot observe a child `exit 1`, and the clone-dir assertion passes vacuously when a failure precedes directory creation — `$LASTEXITCODE | Should -Be 0` added, matching the single-repo sibling); the ReplaceOnly success path exited without `Complete-RunLog` (added, mirroring the final path); and the fourth finding was this bullet's own **99/330** counts, stale after `ad48b21` — corrected to **103/334** with the gate re-run to confirm (334/334, coverage unchanged). Reused **in place**, not imported: `common-auth.ps1`, `create-dispatch-issue.ps1`, `import-labels.ps1` (this repo's copies are equal-or-newer; the moved scripts resolve them via `$PSScriptRoot`, so they bind locally automatically). **Three behaviour changes (all `b37b607`)**: (1) new `-PlanDocsRoot` (default `(Join-Path $PSScriptRoot '..' '..' 'workflow-launch2' 'plan_docs')`) and both `$PlanDocsDir` and `$CloneParentDir` now resolve from `$PSScriptRoot`, not the CWD — the pipeline no longer has to be invoked from the launcher root, and a missing slug directory throws up front naming the resolved root plus the override; (2) new `Test-OwnerVisibilityPolicy` in `repo-functions.ps1`, called by both entry points before any `gh` call and under `-DryRun` too — bail unless `($Owner -eq 'intel-agency' -or $Visibility -eq 'public')`, because `intel-agency` is an Organization on the enterprise plan while `nam20485` is free-tier, where private repos get no Actions minutes so a private clone's dispatch workflows could never run (the wrapper's `-Owner` default was also corrected from `nam20485` to `intel-agency` to match its own help text); (3) the legacy project-setup trigger block and the `-TriggerProjectSetup` / `-SkipProjectSetup` parameters deleted from `create-repo-with-plan-docs.ps1` — its only caller always passed `-SkipProjectSetup $true`, and leaving the block would have silently `Write-Warning`-skipped a workflow whose script did not move. **Verified** (figures re-verified after review round 2, 2026-09-15): `tests/` is new at the repo root, 4 suites / **103 tests green with AND without `GEMINI_API_KEY`**; the gate now reports **334 tests (231 pre-existing + 103 new), 0 failed**, with coverage unchanged at **94.40% over the same 1233 analyzed commands** (`tests/` is in `Step-Test`'s `$testPaths` but deliberately not `$coveragePaths`), and full `-Step all` is green (dotnet 96.47%, python 182 @ 97.67%, e2e all scenarios). The **first full end-to-end `-DryRun` walk** of the wrapper then exited 0 through all five stages (32 labels reported as pending creates; stage 5 shows the `gh-issue-tracking-init` issue with body `/gh-issue-tracking-init` labelled `gh-issue-tracking:direct-body`) — which is what exposed the `import-labels.ps1` defect fixed in `982eef8`: its `-DryRun` still called `gh api repos/<repo>/labels`, and the 404 body parsed to an object with no `name`, so under `Set-StrictMode` stage 4 crashed and stages 4–5 had never been dry-runnable. **Latent CI bug found** in the imported `create-repo-with-plan-docs` suite: its DryRun integration test reaches `New-RepoSecret -SecretName GEMINI_API_KEY`, which read the env var and threw *before* the DryRun stub — it only ever passed because the variable happened to be set on the dev machine, so it would have gone red on a clean runner. The PR #20 review round fixed it **at the root** in `ad48b21`: `New-RepoSecret` warns instead of throwing under `-DryRun`, and the integration test now deliberately *unsets* the variable for the walk. **Labels**: `.github/.labels.json` (32) *contains* the dispatch vocabulary `src/webhook_receiver/filters.py` + `prompt_builder.py` match on (`orchestration:*`, `implementation:ready|complete`, `gh-issue-tracking:direct-body`) but is a **superset** — `agent:*`, `state:*`, GitHub defaults and bare hierarchy labels are matched by nothing (an earlier "exactly" claim here was corrected by the PR #20 review); `intel-agency/agent-context` ships no labels file (404 confirmed), which is why the pipeline sources it from the launching repo — now this one. **Deliberately NOT merged** with `.agents/skills/gh-issue-tracking-init/assets/labels.json` (19 planning labels; 28/15 differ each way, 3 colour conflicts) — reconciliation stays an open follow-up (§9). **Honest scope note**: the `workflow-launch2` teardown is **plan only** — that repo was not touched, and its `plan_docs/` remains the slug-indexed store the pipeline reads.
- **Natural-language orchestration skill — plan drafted (2026-09-15)**: owner brainstorm at [`docs/plans/skill-plan.md`](../docs/plans/skill-plan.md) rewritten into a high-level plan with options + open questions after a full entry-point investigation. Key finding: the `PromptInfo` queue has exactly one producer (`POST /webhooks/github`; envelope `source` is `Literal["github_webhook"]` — no admin/enqueue endpoint); the scripted slug path feeds it *indirectly* by filing a `gh-issue-tracking:direct-body` dispatch issue; the swarm skills never touch the queue. Plan recommends a new `.agents/skills/<name>/` skill (Option A: knowledge + launch, user-confirmation gates, status querying read-only) and explicitly rejects folding into `swarm` and adding an admin enqueue endpoint. Open questions: five of the seven have inline owner answers in the plan (Q1 name `/orchestrate-swarm`; Q3 status querying in scope; Q4 references "skill"; Q5 one confirmation gate; Q7 new label set only, legacy dispatch labels out); genuinely open are Q2 initiation scope (slug path only vs. the generic dispatch path) and Q6 ZCode-local routing (owner asked what it means).
- **`scripts/query.ps1` review-reply route fix (2026-09-15, branch `dev/query-reply-route`)**: the `-ReplyEach` reply route was missing the `pulls/<pr>` segment — `repos/<o>/<r>/pulls/comments/<id>/replies` 404s on POST while the legacy read alias still answers GET, which masked it — so every reply post failed while the GraphQL list/resolve paths worked; the route now inserts the PR number the function already carries. Found while resolving PR #20's second review round (that round's replies posted via the corrected route one-off, flagged in the round summary comment); applied word-for-word across the chain (`intel-agency/agent-context` → `nam20485/swarm-context` → this repo), independent branches + reviewed PRs per the MCP-cleanup precedent.
- **Dispatch-into-live-checkout (2026-09-16, branch `dev/acp-clone-workspace`, plan [`docs/plans/dispatch-in-live-checkout.md`](../docs/plans/dispatch-in-live-checkout.md))**: owner decisions — dispatched sessions run **sandbox-less in the launcher-minted clone**, and the clone primary becomes a new **swarm-agent** (king; runs dispatches himself — init skill is a solo run, never swarmed; escalates to subagent swarms only for genuinely large work; template `orchestrator` stops being `default_agent`). Research settled: clones set `default_agent: "orchestrator"` (coordinator bash-deny forces pointless delegation — the pivot's motivation); skills resolve from `.agents/skills/` via the skill tool (slash palette is TUI-only); `gh-issue-tracking-init` targets the cwd repo by default (cwd-is-clone is load-bearing); ACP host answers permission asks programmatically (old interactive-freeze can't recur). This PR: `ACP_CLONE_ROOT` seam (session cwd = `<root>/<repo name>`, fail-closed, deny-file skipped outright — the lived-in checkout is never written into, seeded config or not), compose pins `ACP_ENABLED=false` (tonight's observed defect: the container attempted ACP with no opencode in the image), docs/env surface synced, +4 tests (186 green). **Kilo review round (2026-09-16, 5 findings — all fixed `13b7bf1`/`be1d0f5`, every thread replied + resolved)**: `SANDBOX_ENABLED`+`ACP_CLONE_ROOT` now fail fast in `AcpHost.__init__` (exclusivity enforced, not just declared; both knobs annotated in `.env.example`/README), deny `opencode.json` never written into a clone-root checkout even when config-less (runtime permission policy is the backstop; docstrings now say bare-workspaces-only), worktree `.git` accepted as a file (`.exists()`), the missing-checkout path's no-`agent_finished` asymmetry pinned by test, and the skill-plan memory bullet corrected (5/7 questions answered inline in the plan; only Q2 initiation scope + Q6 ZCode-local routing genuinely open). Now 189 python tests @ 97.57%, full gate green; merge owner-gated. **Review round 2 (2026-09-19, 2 findings — both stale-comment follow-ups to round 1, fixed `4d74398` after the owner merged `development` into the branch)**: the `Settings.acp_clone_root` knob comment and the M1 plan step still carried the self-managed-config qualifier — reworded to the skip-outright rule; the memory bullet's own stale qualifier caught in the same sweep. CI green; 0 unresolved threads. Follow-ups parked in the plan doc: swarm-agent seeding via launcher transforms (this repo, no template surgery), host-run cutover (funnel repoint open question), fire M1 on issue #1 relabel, M2 worktree-per-impl deferred. **Dual agent environments (owner, 2026-09-16)**: exactly two mutually exclusive modes — clone (main checkout, no sandbox/worktree) or sandbox — chosen per-repo at init, never dynamic, never mixed; the launcher stamps both representations at creation (GitHub repo variable `AGENT_ENVIRONMENT` for the service's per-envelope workspace resolution, fail-closed when unknown; an AGENTS.md instance line for in-repo readers); worktree-per-impl is not a third environment (clone-mode refinement at most). Open: sandbox-mode criteria + new-repo default.
- **PR #21 (development→main) review rounds resolved (2026-09-20, direct commits to `development` — the merge PR's own head)**: two kilo-code-bot rounds on the merge PR (13 + 16 findings; all 29 threads replied + resolved; `required_review_thread_resolution` on the dev/main ruleset makes this a merge precondition). **Python/ACP**: the `SANDBOX_ENABLED`+`ACP_CLONE_ROOT` exclusivity now also fires for an *injected* bridge (the DI seam) and at `from_env` boot (so it surfaces even with `ACP_ENABLED=false`; `AcpHost.__init__` kept as defense in depth); clone-root resolution validates the repo-name segment (`[A-Za-z0-9._-]+`, rejecting `.`/`..` traversal via `acp_smoke --repo`) and verifies checkout identity (`git remote get-url origin` vs envelope repo — same-named forks can no longer receive each other's sessions); `ACP_WORKSPACE_ROOT` gets `expanduser()` like `ACP_CLONE_ROOT`; deny-config skips (clone-root and manages-own-config) now `logger.warning` because `ACP_DENIED_TOOLS` is enforced *only* by that file; `_prepare_workspace` docstring headline no longer claims "never the service's own repo" unconditionally. **PowerShell launcher**: the amended seed commit is force-pushed as `origin HEAD` (not `HEAD:main` — the agent-context template's default branch is `development` and generated repos have no `main`; wrapper + stage-1 recovery push + AST contract test aligned), with `$LASTEXITCODE` guards added after `trigger-gh-issue-tracking-init.ps1` (wrapper) and `create-dispatch-issue.ps1` (trigger script); `Invoke-External` grew `-InputText` (stdin) + `-RedactValues` so `New-RepoSecret` passes the `GEMINI_API_KEY` value via `gh secret set` stdin — never argv, never verbose/failure messages; `Copy-PlanDocs` takes `-DocsSubDir` instead of reaching for the caller's `$docsDir`; `Invoke-GitCommitAndPush` guards the `$null.Trim()` on empty `branch --show-current`; the editor-launch block skips under `-DryRun` and requires `Get-Command code-insiders`; the dispatch-labels existence check keys on `$LASTEXITCODE` alone (body-sniffing `'"message"'` broke idempotency); `cleanup-template-state` no longer assigns the automatic `$Matches`; `strip-model-settings` also trims dangling commas before trailing `//` comments (quote-free comment tail only); unused `$preFiles` snapshot dropped. **Test-suite defects fixed (the round-1 CRITICAL was a real-repo hazard)**: the error-handling test ran the SUT *without* `-DryRun` — on an authenticated machine it created real public repos; it now triggers the owner/visibility policy guard under `-DryRun`, which moved **inside** the `try` so the catch's FAILED/exit-1 contract applies to it; the assertion-free ReplaceOnly test became observable and exposed a **latent `Update-TemplatePlaceholders` bug** — `$path.DirectoryName` doesn't exist on DirectoryInfo (terminating under common-auth's `Set-StrictMode`), replaced with `[System.IO.Path]::GetDirectoryName`; `New-RepoSecret`/`Invoke-External` tests assert stdin+redaction; the logging test asserts the elapsedSeconds value, not its JSON-round-trip type. **Docs**: the merged fold plan moved to `docs/plans/completed/` (status header → COMPLETE) with all inbound refs repointed (3 scripts, completed/orchestration-cycle doc, this memory); the plans index now also lists `dispatch-in-live-checkout.md`; skill-plan-review's "Three of the seven Open Questions" corrected to Four (per its own finding #15). Verified: 194 python tests (was 189) + 106 Pester tests (was 103), full `validation.ps1 -Step all` green. **Review round 3 (2026-09-21, 5 findings — all fixed, every thread replied + resolved; commits `711ef35`/`dc7d5c1`/`c759d2f`/`fb1db41`/`18e7ca9`)**: the clone-identity `git remote get-url` probe was a synchronous `subprocess.run` directly on the event loop (10s service-wide freeze worst case) — `_checkout_is_repo`/`_prepare_workspace` are now async with the probe in `asyncio.to_thread`; **the round-2 `-InputText` stdin fix was itself broken** — pwsh's pipe terminates every piped object with a newline, so `gh secret set` stored the secret with a trailing `\n`; the branch now writes exact bytes via `Process.StandardInput` (still no argv, and no temp file — `--body-file` declined to keep secrets off disk), with a trim-free length assertion (len=17, not 16 — "stdin-payload-123" is 17 chars) plus an exit-code-propagation test, byte-exactness proven by a standalone hex dump; `dispatch-in-live-checkout.md`'s own Status header still said PLAN while the index said LANDED (synced); `test_acp_env_overrides` now `delenv`s the ambient sandbox knobs; compose header says "(default false)" not "pinned false" (kept the `${ACP_ENABLED:-false}` forward — a hard pin would silently swallow an explicit operator override). Verified: 194 python + 107 Pester (was 106), full gate green. **Review round 4 (2026-09-22, 2 findings on the round-3 stdin branch — both fixed in `7fddb5d`, threads replied + resolved)**: the `ReadToEndAsync` drains now start before the stdin write (a payload over the pipe buffer plus a child writing before reading would have deadlocked the parameterless `WaitForExit`), and stdout/stderr are split separately before concatenating (the raw-string concat fused stdout's last line with stderr's first when stdout lacked a trailing newline — pinned by a no-newline failure-message test). Verified: 194 python + 108 Pester, full gate green. PR #21 then merged to `main` (owner admin-bypass authorization, same session).
- **MCP server discontinuation (2026-09-12, branch `dev/mcp-cleanup`)**: owner verdict — sequential-thinking and memory-graph MCP servers are redundant and inferior to the model's builtin reasoning and memory. Defs removed from `.zcode/config.example.json` + gitignored `.zcode/config.json`, and from `.opencode/opencode.jsonc` (both the `mcp` blocks and the `permission` entries); guidance withdrawn from AGENTS.md (Tool Usage), `.agents/rules/tools.md` (two usage sections replaced by a Discontinued note), `.agents/rules/practices.md` (Memory Tools orientation bullet), `.opencode/agents/orchestrator.md` (two planning-discipline bullets). **The identical change (word-for-word, so future upstream merges see both-sides-same-change and stay conflict-free) was applied in the parent (nam20485/swarm-context, PR #5 — new `maintenance` milestone there) and grandparent (intel-agency/agent-context, PR #25, no milestone — repo has none)** per owner direction — independent branches in each repo's own clone, no upstream pull/graft (the grandparent link stays parked). All three PRs CI-green 2026-09-12; all merged (this repo `8a92019`, parent `c2bf74d`, grandparent `7088968`).

- **Webhook dispatch outage on `gap-miner-v2-papa26` triaged (2026-09-15, live ops — chain verified/fixed locally, owner-side install step remains)**: dispatch issue #1 fired nothing. Probed in order: funnel→Caddy→receiver path healthy (`/health` 200 via `https://precision5820.tail11ba79.ts.net`, unsigned POST 401 proves reachability); then the receiver's `OS_WEBHOOK_SECRET` in `.env` was a stale ~19-char value with trailing whitespace while the real App secret is the 64-char `OS_WEBHOOK_SECRET` in `~/.api-keys-export.sh` — `.env` synced (backup `.env.bak-20260915`), receiver force-recreated, locally-signed probe → `pong` 200. The owner's redelivery from the App web UI arriving at the receiver (401 pre-fix) proves the app's webhook URL already points at the funnel. **Root cause settled 2026-09-16 (doc-verified)**: `orchestrator-service-webhook` is a **private** app owned by the nam20485 personal account — GitHub docs: a private app "can only be installed on the account that owns the app", so it is structurally un-installable on intel-agency by anyone (the "… is a private GitHub App" page the org owner saw IS that rule, not an error). intel-agency is now **enterprise** plan (API-confirmed; owner moved it); nam20485 remains org admin. The owner's "it used to work on intel-agency" = the launcher leg (PAT-driven repo create/clone — app-independent), never the webhook leg; app e2e always ran on nam20485-account repos (Phase-4 demo: `nam20485/swarm-orch-demo-scratch`). **Fix options**: (a) make the app public ("Any account") in nam20485's Developer settings → org-owner installs with All repositories (enterprise GitHub-App policy must allow it); or (b) add an **org-level webhook** on intel-agency (org Settings → Webhooks: URL `https://precision5820.tail11ba79.ts.net/webhooks/github`, content-type JSON, secret = the 64-char value, Issues events) — the receiver verifies/gates org webhooks identically and every org repo is covered with no installation dance. Red herrings documented so they don't bite again: `opencode-agent` is an unrelated public app that once commented on the old repo (public `/apps/opencode-agent` shows `["issue_comment"]`-only events — irrelevant to this pipeline); `kilo-code-bot` is a separate app covering `nam20485/swarm-orchestration-service` whose webhook points at the funnel with the OLD short secret (its 202 pre-fix is how the stale secret survived undetected). Tooling notes: no App private key on this machine → App-JWT endpoints (`/app/installations`, `/app/hook/deliveries`, `/repos/*/installation`) unreachable via gh PATs; and even after installs are fixed, `DIRECT_BODY_ALLOWED_SENDERS` is still empty (fail-closed) so direct-body dispatch enqueues nothing until the owner's login is allowlisted in `.env` (+ receiver recreate) — and compose mode is listener-only (in-memory queue, no ACP consumer); real runs need the host-run mode. **Resolution (2026-09-16)**: the App was made public and installed (its `installation_repositories added` delivery arrived, signature-verified, 202) AND an **org-level webhook** was added on intel-agency (hook id 680116756; ping → 200 `pong` proves secret parity) — org webhook is the Issues route (App must keep Issues UNchecked or every labeled issue double-delivers: dedup keys on delivery_id, which differs per source). Real `issues.labeled` deliveries from the org now arrive and pass HMAC; the sender gate bit once by design (owner relabeled from the org account `intelagentone`) — allowlist now **`nam20485,intelagentone`** (scripted PAT sender + manual org-owner sender). End-to-end accept (202 → `prompt_queued`) pending one more relabel confirmation.

## Completed Work Items

- **Plans-dir cleanup (2026-09-11, PR #16 merged `7656587`)**: landed plans archived into `docs/plans/completed/` (simplification — header now reads COMPLETE, integration, promptinfo-design, phase4-bridge-design); top level keeps only open docs (`old-stack-decommission.md` PLAN ONLY owner-gated, `grandparent-upstream-graft.md` PARKED trigger-gated, `turing-alpha-bot-account.md` PROPOSED owner decision) plus `SWARM_PARALLEL_COMPILATION_PLAN.md` (upstream template surface — exists in `upstream/development`, referenced by `.zcode/agents/swarm-orchestrator.md`; left in place to keep upstream merges mechanical). New `docs/plans/README.md` index records the layout and move-on-completion convention. All inbound references repointed (AGENTS.md, README.md, compose.yaml, `src/webhook_receiver/` README + 5 source-file comments, this memory); A2A research links unlinked (that doc was never committed). Repo hygiene: milestones `bootstrap`/`phase-0`..`phase-5` closed, open `maintenance` milestone created (this PR rode it).
- **Phase 0.1 — ACP spike** (PR #3, merge `719c009`, CI green; cold-verified by coordinator re-runs): round-trip PASS on `agent-client-protocol==0.12.1` + opencode 1.18.30 (`initialize` → `session/new` → prompt → `session/update` stream → `end_turn`); **deny paths PASS** — host auto-`reject_once` on `session/request_permission` → tool `failed` → `end_turn` 94 ms later, no hang; `permission.bash:"deny"` intercepts pre-prompt (zero requests, "unavailable tool"). §6 unknowns resolved: `acp` floor **v0.15.10**; `--auto` does not exist for `acp` (permission is config + host responses only); `--port/--mdns/--cors` = HTTP companion endpoint (default port 0 = stdio only). Spike: `spikes/acp/` (re-run steps in its README). Notable: config-deny is *pre-prompt interception*, stronger than expected — Phase 2 can use it as the primary fail-closed layer.
- **Phase 0.0 — listener port + compose skeleton** (PR #2, merge `cdb3d4d`, CI green): `src/webhook_receiver/` — FastAPI app (verify 401 → ping 200 → JSON 400 → size 413 → store → gate), HMAC `github.py` verbatim from @`2bd6d06`, `should_dispatch` gate incl. fail-closed direct-body allowlist, SSE `EventStore`; pytest 47 @ 94.79%; compose = webhook-receiver + digest-pinned Caddy (`/webhooks/github` + `/health` only, 404 catch-all; **no** `orchestratorservice` container); `validation.ps1` python branch (venv bootstrap, ≥85% coverage gate, graceful skip). Dispatch stubbed → Phase 1.
- **Bootstrap** (PR #1, merge `06672c5`): clone de-contaminated (parent Class 2 plan docs pruned, swarm-metrics scaffold, memory reset, workspace file renamed); base grafted onto `upstream/development` `0d9c7dd` (fork-style per Decision 8 — template-route seeds land squashed; fixed via `remote add` + fetch + `reset --soft` + force-push while trees matched); branch-protection ruleset recreated (deletion + non-ff + 1-approval PRs, admin bypass); milestones `bootstrap`/`phase-0`.

## Decisions

- **Sequential-thinking + memory-graph MCP: use discontinued** (owner, 2026-09-12): both deemed redundant and inferior to the model's builtin reasoning and memory — tools removed and guidance withdrawn across the template chain (this repo, parent swarm-context, grandparent agent-context); do not call their tools, re-add their defs, or restore the guidance.
- **A2A protocol: rejected — not a fit** (owner, 2026-09-10; research findings recorded above under Current Activity — the research doc was never committed): anti-fit inside the ZCode swarm (wire protocol can't lift the depth-1 subagent limit; workers stay least-privilege; the file board already covers co-tenancy) and for local opencode control (ACP's `request_permission` + tool-call streaming are the fail-closed properties A2A lacks). The optional facade-on-FastAPI path existed but the owner declined it — dropped entirely, not trigger-gated. Do not re-research or re-propose without owner direction.
- **Repo linking: fork-style upstream remote** (plan §5 Decision 8): swarm-context stays the swarm-harness source of truth; this repo merges `upstream/development` on demand, each sync a reviewed PR. Keep edits to shared template files (README, `.agents/rules/`, `.zcode/agents/`, AGENTS.md) minimal and upstream of product code so merges stay mechanical.
- **ZCode MCP config cannot be committed with secrets** (verified 2026-09-10): `config.json` supports no env-var interpolation — `env`/`headers` values are literal; http servers get no env fallback (endpoint 1001s without the header). Pattern: real `.zcode/config.json` gitignored; `.zcode/config.example.json` committed. OpenCode's `{env:VAR}` is why `.opencode/` config can be committed.

## Remember To Do

- **Issue-dependency webhook events as a future orchestration trigger** (owner, 2026-09-16): the GitHub "Issue dependencies" event ("blocked by"/"blocking" added or removed) is deliberately left unsubscribed; revisit when the event handler grows a real state machine — e.g. dependency-gated story/epic dispatch order. Cheap to adopt later: subscribe in the webhook/App settings; the receiver already 202-and-drops unknown events until the gate is taught to match.

- **`thoughtLevel: off` blocks GLM-5.3-Flash worker spawning entirely** (carried from upstream, observed 2026-09-10): either drop it from the worker defs, pin worker models that support off, or get harness support. Resolve before the next swarm run.
- **Align `swarm-state.ps1` budget with concurrent-not-all-time semantics** (carried from upstream): track in-flight (pending/running) tasks; update `.agents/rules/swarm.md` § Budget + the skill.
- **Per-run telemetry archiving** (carried from upstream): automate copying worker model-io JSONLs to `.swarm/<run-id>/telemetry/`.
- **SwarmSandbox AppHost never run end-to-end by any gate** (carried known-gap): manual `dotnet run` smoke when next needed.
- **Projects board** still unset on this repo (token lacks `read:org`; milestones + ruleset done). PRs currently carry milestone only.
- **GITHUB_TOKEN consolidation — DEFERRED by owner (2026-09-10)**: unknown dependents rely on the env token values; owner won't break things to map them. Temp fix accepted (unset in shell → keyring governs there; dotfiles untouched). Do not delete/align the PATs or edit dotfiles without owner direction; if full gh scopes are needed under shadowing, add scopes to the env PAT instead of removing it. Optional PAT rotation still stands (one value hit a session log once during diagnosis).
- **Grandparent sync (`intel-agency/agent-context` → `swarm-context`) — PARKED, trigger-gated (owner 2026-09-11)**: `upstream` remote added to the parent's local clone (fetch-only, push DISABLEd, fetched once). Parent is a webui-style squashed seed — root `1375d86` not in grandparent, tree exactly matches grandparent `ba935fc4` (27 commits behind it); first real sync needs graft surgery that cascades here (we share that root). Full plan, commands, and verification: [`docs/plans/grandparent-upstream-graft.md`](../docs/plans/grandparent-upstream-graft.md). Execute only when we need something from the grandparent.
- **`workflow-launch2` teardown + label reconciliation — PLAN ONLY, owner-gated (2026-09-15)**: the fold moved the launcher's pipeline into this repo but deliberately left `nam20485/workflow-launch2` untouched; it still holds the `plan_docs/` slug store the pipeline reads via `-PlanDocsRoot`. Open follow-ups, all in [`docs/plans/completed/fold-workflow-launch2-into-service.md`](../docs/plans/completed/fold-workflow-launch2-into-service.md) §9: (a) reduce that repo to `plan_docs/` + a README declaring it a slug-indexed key-value store — blocked on deciding whether `.INTAKE` / `.NEW` / `.wip` / `templates` are slugs or index metadata (they are not slugs), and on where the intake→slug promotion flow goes afterwards; (b) reconcile `.github/.labels.json` (32 dispatch labels) with `.agents/skills/gh-issue-tracking-init/assets/labels.json` (19 planning labels) now that both live here — 28/15 differ each way with 3 colour conflicts; (c) decide whether the app-plan authoring side (`plan_docs/templates/`, the launcher's `docs/agent-creation-instructions.md` and `docs/new-ai-app-creation-templates/`) also folds in. Do not start any of these without owner direction.
- **`GEMINI_API_KEY` value was echoed into a session transcript (2026-09-15)**: a malformed shell check (`${VAR:+YES}${VAR:-NO}`) printed the live value instead of just its presence, during the fold's verification. It was not written to any file, committed, or repeated, but it did land in the transcript. Rotation recommended — same class of incident as the `GITHUB_TOKEN` note above. Also note the pipeline *requires* this variable at launch time (`New-RepoSecret` reads it from the environment), so rotating it means re-exporting it wherever launches are run.
