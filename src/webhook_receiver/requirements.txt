# Runtime dependencies for the webhook-receiver listener (Phase 0.0) and
# ACP host (Phase 2). Pinned exactly; bump deliberately.
agent-client-protocol==0.12.1
fastapi==0.141.1
uvicorn[standard]==0.52.4
